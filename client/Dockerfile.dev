FROM oven/bun:latest

WORKDIR /app

COPY package.json bun.lock ./

# Copy ALL workspaces (bun needs all of them to resolve)
COPY server/ ./server/
COPY client/ ./client/
COPY packages/ ./packages/

RUN bun install

WORKDIR /app/client

EXPOSE 5173

CMD ["bun", "run", "dev", "--host", "0.0.0.0"]