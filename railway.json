{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "bun install && cd server && bunx prisma generate"
  },
  "deploy": {
    "startCommand": "cd server && bun run start",
    "healthcheckPath": "/health"
  }
}
